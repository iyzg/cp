#include <bits/stdc++.h>

using namespace std;


// WRite distance formula up here to find best route
int main() {
    int n;
    scanf("%i", &n);
    
    double points[n][2];
    for (int i = 0; i < n; i++)
    {
        scanf("%lf %lf", &points[i][0], &points[i][1]);
    }


}

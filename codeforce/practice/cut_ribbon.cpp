#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, a, b, c;
    cin >> n >> a >> b >> c;

    int maximum_pieces[n] = { 0 };
    
    for (int i = 0; i < n; i++)
    {
        
    }

    cout << maximum_pieces[n - 1];
}
